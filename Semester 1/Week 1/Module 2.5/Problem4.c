// Starting in the name of Almighty Allah

#include <stdio.h> 

int main ()
{
    int in;
    
    scanf ("%d", &in);

    if (in > 0)
    printf ("positive");

    else if (in < 0)
    printf ("negative");

    else 
    printf ("zero");
}