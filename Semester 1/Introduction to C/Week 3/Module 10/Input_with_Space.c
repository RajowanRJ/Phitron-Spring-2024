// Starting in the name of Almighty Allah
#include <stdio.h>
#include <string.h>

int main ()
{
    // Method 1 (if the Array capacity Exits it won't be printed)
    char a[100];
    gets(a);
    printf("%s", a);
}